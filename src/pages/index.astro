---
/* @ts-nocheck */
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Player from '../components/Player';
import AlbumCard from '../components/AlbumCard';
import { getCollection } from 'astro:content';

const albums = await getCollection('albums');
const albumEntries = albums.map((a) => ({ id: a.id, ...a.data }));

// Force SSR so OG tags are built from the actual request origin (Workers/preview)
export const prerender = false;
---

<!doctype html>
<html lang="en">
	<head>
    <BaseHead
      title="Loftwah FM — My SunoAI music player"
      description="My personal SunoAI music player at fm.loftwah.com — stream my generated tracks and videos on phone or desktop."
      image="/fm-og.jpg"
      ogType="music.playlist"
      ogAuthor="Loftwah"
    />
	</head>
	<body>
		<Header />
        <main class="space-y-6 mx-auto max-w-6xl px-4 mt-6 mb-28">
            <section class="panel p-3">
                <p class="text-sm">
                    My personal SunoAI music player. Browse albums, play tracks and videos, and share links that generate rich previews.
                </p>
            </section>
            {albumEntries.length > 1 ? (
                <section class="grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-4">
                    {([
                      { id: 'all', slug: 'all', title: 'All Songs', artist: 'Loftwah', year: new Date().getFullYear(), cover: '/all-songs.jpg', tracks: [] },
                      ...albumEntries,
                    ]).map((a) => (
                        // @ts-ignore Astro client directive
                        <AlbumCard album={a as any} client:idle />
                    ))}
                </section>
            ) : albumEntries.length === 0 ? (
                <p>No albums found.</p>
            ) : null}
            <!-- @ts-ignore Astro client directive -->
            <div id="player">
              <Player albums={albumEntries as any} client:load />
            </div>
        </main>
		<Footer />
	</body>
</html>
